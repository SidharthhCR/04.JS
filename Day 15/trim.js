let str='           I Love Mearn Stack         '
console.log(str)
let newstr=str.trim();//its used to cut the white space in front(leading) and back(trailing) of a exisiting string
console.log(newstr);