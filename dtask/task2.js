//Given [1, 2, 3, 4, 5], use map() to create a new array with each number 

let numArray=[1, 2, 3, 4, 5];
let newArray=numArray.map((eachElement)=>eachElement )
console.log(newArray)
//Sort the array [45, 12, 89, 33, 25] in ascending order using sort().
numArray.sort((eachElement))