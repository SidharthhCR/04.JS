//multiplicates each elements in the array with 5 and store it in a new array

let numArray=[5,8,7,6,10,42,40];
let newArray=[];

let 
for(let i=0;i<=numArray.length-1;i++){
    mul*=numArray[i];
    newArray.push(mul);
    
}
console.log(newArray)